using Microsoft.VisualStudio.TestTools.UnitTesting;
using ServiceStack.Service;
using ServiceStack.ServiceClient.Web;

namespace Phytel.API.DataDomain.Scheduling.Services.Test
{
    [TestClass]
    public class User_ToDo_Test
    {
        //[TestMethod]
        //public void Post_ToDoByID()
        //{
        //    string url = "http://localhost:8888/ToDo";
        //    string ToDoID = "??";
        //    string contractNumber = "InHealth001";
        //    string context = "NG";
        //    string version = "1.0";

        //    //Set the value of this call to a valid ContactId (Contact Collection _id field) from the contract database
        //    IRestClient client = new JsonServiceClient();

        //    GetToDoResponse response = client.Post<GetToDoResponse>(
        //        string.Format("{0}/{1}/{2}/{3}/ToDo/{4}", url, context, version, contractNumber, ToDoID),
        //        new GetToDoResponse() as object);

        //    Assert.AreEqual(string.Empty, string.Empty);
        //}
    }
}
