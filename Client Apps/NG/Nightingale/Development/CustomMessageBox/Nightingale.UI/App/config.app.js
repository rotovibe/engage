define([],
    function () {

        var title = 'Nightingale';
        var remoteServiceName = 'api';

        var config = {
            title: title,
            remoteServiceName: remoteServiceName
        };
        return config;
    });